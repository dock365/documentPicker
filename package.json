{
  "name": "@dock365/documentpicker",
  "version": "0.0.1",
  "description": "Document picker for sharepoint with fabric ui",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist/*",
    "build": "npm run clean && tsc",
    "build-watch": "tsc --watch",
    "patch-release": "npm run build && npm version patch && npm publish --access=public && git push --follow-tags",
    "minor-release": "npm run build && npm version minor && npm publish --access=public && git push --follow-tags",
    "major-release": "npm run build && npm version major && npm publish --access=public && git push --follow-tags",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dock365/documentPicker.git"
  },
  "keywords": [
    "react",
    "document",
    "picker",
    "fabric",
    "sharpoint",
    "spfx"
  ],
  "author": "Hafeez Hamza <hafeez1042@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/dock365/documentPicker/issues"
  },
  "homepage": "https://github.com/dock365/documentPicker#readme",
  "devDependencies": {
    "@pnp/common": "^1.3.5",
    "@pnp/logging": "^1.3.5",
    "@pnp/odata": "^1.3.5",
    "@pnp/sp": "^1.3.5",
    "@types/react": "^16.9.2",
    "@types/react-dom": "^16.9.0",
    "office-ui-fabric-react": "^6.202.0",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "ts-node": "^8.3.0",
    "tslint": "^5.19.0",
    "typescript": "^3.5.3"
  }
}